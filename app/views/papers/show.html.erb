<p>
  <strong>Name:</strong>
  <%= @paper.name %>
</p>

<% @paper.template.sections.each do |section| %>
	<p><%= link_to "#{section.name}", "/contents/#{@paper.id}/#{section.id}" %></p>
<% end %>

<p></p>

<%= link_to 'Edit', edit_paper_path(@paper) %> |
<%= link_to 'Back', papers_path %>
